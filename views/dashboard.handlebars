    <div class="mb-5 pt-3">
        <a href="/dashboard/create/"><button id="create-propiety" class="button is-primary is-pulled-right">create a new propiety</button></a>
        
        <h2 class="title pb-4">dashboard: your propiety</h2><hr>
{{#if propieties.length}}
    </div>

    {{#each propieties as |propiety|}}
        {{> propiety-info propiety}}
        
            <a href="/dashboard/edit/{{propiety.id}}"><button class="button mb-5 ml-5 is-warning">edit propiety</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">you do not currently have any propiety</h6>

{{/if}}